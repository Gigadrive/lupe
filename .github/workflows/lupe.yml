# Dogfood: lupe reviews its own pull requests.
# Requires an ANTHROPIC_API_KEY secret (org-level, shared to this repo) — the
# job fails until that secret exists. See apps/action/README.md.
name: lupe

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [main]

permissions:
  contents: read # read the repo
  pull-requests: write # post the review + sticky summary

concurrency:
  group: lupe-${{ github.event.pull_request.number }}
  cancel-in-progress: true # cancel superseded runs on a new push

jobs:
  review:
    runs-on: ubuntu-latest # GitHub-hosted: guarantees the node24 action runtime
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5 # default fetch-depth: 1 — the diff comes from the API, not git history
      - uses: Gigadrive/lupe/apps/action@c80a4dd311ffec15b074175eb9867a41fd1fbabe # v0.2.0 # pin immutable during the pilot; move to @v0 once trusted
        with:
          provider: anthropic
          profile: chill
          max-cost-usd: '2'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
