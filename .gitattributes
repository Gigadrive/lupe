# Normalise all text to LF in the repo and on checkout, so the committed
# Action bundle (apps/action/dist) never drifts due to line-ending differences.
* text=auto eol=lf

# Generated bundle — keep it out of diffs and language stats.
apps/action/dist/** linguist-generated=true
pnpm-lock.yaml linguist-generated=true
