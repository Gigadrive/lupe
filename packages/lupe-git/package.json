{
  "name": "@gigadrive/lupe-git",
  "version": "0.0.0",
  "description": "Diff parsing, hunk → (line, side) anchoring, read-only repo tools, and Qodo-style context compression for lupe.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc",
    "test": "vitest run --passWithNoTests",
    "check:exports": "publint --strict",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@gigadrive/lupe-core": "workspace:*",
    "simple-git": "catalog:"
  },
  "peerDependencies": {
    "effect": ">=3.21.0 <4",
    "@effect/platform": ">=0.96.0 <0.97"
  },
  "devDependencies": {
    "effect": "catalog:",
    "@effect/platform": "catalog:",
    "@types/node": "catalog:",
    "@repo/typescript-config": "workspace:*"
  },
  "engines": {
    "node": ">=20.18.0"
  }
}
