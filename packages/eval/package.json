{
  "name": "@repo/eval",
  "version": "0.0.0",
  "private": true,
  "description": "Labeled evaluation harness for lupe: precision/recall/F1, noise budget, prompt-cache and SARIF regression gates.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc",
    "test": "vitest run",
    "clean": "rm -rf .turbo coverage"
  },
  "dependencies": {
    "@gigadrive/lupe-core": "workspace:*",
    "@gigadrive/lupe-git": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:"
  },
  "engines": {
    "node": ">=20.18.0"
  }
}
